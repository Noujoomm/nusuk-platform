{
  "name": "nusuk-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "docker compose up -d",
    "dev:down": "docker compose down",
    "dev:api": "cd apps/api && npm run start:dev",
    "dev:web": "cd apps/web && npm run dev",
    "build": "cd apps/api && npx prisma generate && npm run build && cd ../web && npm run build",
    "start": "cd apps/api && npx prisma db push --accept-data-loss && node dist/prisma/seed.js; API_PORT=4000 node dist/main.js & sleep 5 && cd ../web && npx next start -p ${PORT:-10000}",
    "lint": "npm run lint --workspaces --if-present"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
